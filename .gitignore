# Node.js bağımlılıkları
node_modules/

# Ortam değişkenleri dosyaları
.env
.env.local
.env.development
.env.production

# API test istek dosyaları
# Eğer doğrudan requests.http adıyla kaydediyorsanız, bu satırı ekleyin:
requests.http
# Genel olarak tüm .http veya .rest dosyaları için:
*.http
*.rest
# VS Code'daki bazı eklentiler için (örn. Thunder Client)
.vscode/*.http
.vscode/*.rest

# Docker özel dosyaları (eğer varsa ve git ile paylaşmak istemiyorsanız)
# .dockerignore

# Test/Log dosyaları
*.log
npm-debug.log*

# Üretim için derlenmiş frontend dosyaları
build/
dist/